name: 05 - 01 - Event Filters and Actiivity Types

on:
  pull_request:
    types:
     - opened
     - synchronize
    branches:
     - main

jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
            - run: echo "Running whenever a PR is Opened or synchronized AND base branch is main"
